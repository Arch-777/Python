# practical 3

bits = int(input("Enter bits - "))
megabytes = bits / 8 / 1024 / 1024
print(bits, " Bits to MB - ", megabytes)
gigabytes = int(input("Enter GB - "))
terabytes = gigabytes / 1024
print(gigabytes, " GB to TB - ", terabytes)
